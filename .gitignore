# Internal debugging notes — contain a real IP/domain, not for publication
CLAUDE_REVIEW_NOTES.md

# Scratch / local
.tmp/
*.bak
*.bak-*
*.patched
*.log
.DS_Store

# Runtime secrets and generated node state — never publish
.env
.env.*
!.env.example
*.token
*.pem
*.key
inputs.env
node.json
entry-node.json
state/
ssl/
keys/
*.zip
ansible/.venv/
ansible/.cache/
.ansible/
.molecule/
__pycache__/
*.py[cod]

# Session working notes — contain real IPs/keys/domains, not for publication
PROJECT-STATE.md

# Local audit SSH keypair — private key, never publish

# Inventory data with real addresses - never publish
ansible/inventories/test/
ansible/inventories/local/
ansible/inventories/production/hosts.yml

# Real values of this deployment and its secrets - never publish.
# Examples for both live in ansible/examples/.
ansible/playbooks/group_vars/all/vault.yml
ansible/playbooks/group_vars/remnawave_nodes/zz-local.yml

# Collections installed into the checkout by ./setup-controller
ansible/collections/ansible_collections/
