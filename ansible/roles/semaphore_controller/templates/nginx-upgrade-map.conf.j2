# Managed by the semaphore_controller role.
# The WebSocket that carries live task output needs Connection: upgrade passed
# through, and only when the client asked for it. A map belongs in the http
# context, which is why it is here and not in the site file.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
